apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: jespergp/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        infer:
          - "src/**/*.ts"
    - image: jespergp/tickets
      context: tickets
      docker:
        dockerfile: Dockerfile
      sync:
        infer:
          - "src/**/*.ts"
    - image: jespergp/client
      context: client
      docker:
        dockerfile: Dockerfile
      sync:
        infer:
          - "***/*.js"
